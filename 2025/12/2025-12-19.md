## Work Log — 2025-12-19

### Summary
Spent the day stabilizing the CI pipeline and linting configuration for `admin-lite`. The work involved iterative fixes to Yarn cache handling in CI workflows and resolving ESLint/YAML linting issues introduced by `.yarnrc.yml`.

### Tasks & Changes

#### CI Pipeline Stability — Yarn Cache & Dependency Installation
- **Repo:** `admin-lite`
- **What was done:** Iteratively fixed a broken CI pipeline caused by corrupted/stale Yarn cache issues. Switched runner from non-ubuntu to `ubuntu-latest`, disabled and re-tuned cache strategies, and improved the dependency installation process to ensure reliable fresh installs.
- **Commits:**
  - `a9dbd168` — fix(ci): clean Yarn cache before installing dependencies
  - `4a057e2c` — fix(ci): enhance Yarn cache cleaning process in CI workflow
  - `2c5d4ec4` — fix(ci): improve Yarn cache cleaning process to handle corrupted directories
  - `8a462fbf` — fix(ci): enhance Yarn cache cleaning and improve dependency installation process
  - `ec96ba83` — fix(ci): disable Yarn cache and install dependencies from network to improve reliability
  - `04861d05` — fix(ci): update cache cleaning process and improve dependency installation
  - `7a76bee6` — fix(ci): update Yarn cache configuration for fresh installs
  - `9aae8cdb` — fix(ci): switch CI jobs to use ubuntu-latest for improved compatibility

#### ESLint & `.yarnrc.yml` Linting Fixes
- **Repo:** `admin-lite`
- **What was done:** Fixed YAML linting issues in `.yarnrc.yml` — standardized quote usage, corrected `compressionLevel` formatting, and added then removed ESLint disable comments before landing on properly ignoring the file in ESLint config entirely.
- **Commits:**
  - `593e5301` — fix(config): ensure compressionLevel is properly quoted in .yarnrc.yml
  - `8a12f295` — fix(yarnrc): standardize quotes in packageExtensions section
  - `900100be` — fix(yarnrc): remove quotes from compressionLevel in .yarnrc.yml
  - `f47e93d9` — fix(yarnrc): add eslint disable comments for better YAML linting
  - `915845ae` — fix(eslint): remove unnecessary eslint-disable comments from .yarnrc.yml
  - `f2e7082a` — fix(eslint): ignore .yarnrc.yml in ESLint configuration

### Notes
- CI fixes were iterative — worth verifying the final pipeline run is green and stable before closing this out.
- No PRs raised for any of these changes; consider consolidating into a PR for review visibility.