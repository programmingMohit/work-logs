## Work Log — 2025-12-04

### Summary
Work focused on a single fix in `admin-lite` related to questionnaire attachment behavior. The update ensures photos and documents are unchecked by default when attachments are enabled, improving default state consistency and reducing accidental selections.

### Tasks & Changes

#### Questionnaire attachments default-state fix
- **Repo:** `admin-lite`
- **What was done:** Fixed questionnaire attachment toggle behavior so photo and document options are not preselected by default when attachments are enabled.
- **Commits:**
  - `003c23a9` — [FIX][NO-TICKET] fix(questionnaires): ensure photos and documents are unchecked by default when enabling attachments (#168)
- **PR:** [#168 — [FIX][NO-TICKET] fix(questionnaires): ensure photos and documents are unchecked by default when enabling attachments](https://github.com/Nimbly-Technologies/admin-lite/pull/168)