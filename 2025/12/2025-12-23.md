## Work Log — 2025-12-23

### Summary
Spent the day debugging CI pipeline issues in `admin-lite`. Investigated yarn cache corruption problems, iterating between disabling the cache and reverting runner configuration changes.

### Tasks & Changes

#### CI Pipeline Stability — Yarn Cache & Runner Config
- **Repo:** `admin-lite`
- **What was done:** Diagnosed and iterated on yarn cache corruption in GitHub Actions. Disabled the yarn cache to prevent corruption, then reverted an earlier change that had removed self-hosted runners and added cache cleaning steps — restoring the original runner setup.
- **Commits:**
  - `35fede85` — fix(ci): disable GitHub Actions yarn cache to prevent corruption
  - `a58c4c47` — revert(ci): restore self-hosted runners and remove cache cleaning steps