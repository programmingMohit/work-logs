## Work Log — 2025-08-22

### Summary
Worked on a bug fix in `audit-admin` to correct gallery filter API behavior when `groupBy` is set to `all`. The change ensures unnecessary search parameters are removed in that specific case. The update was merged through PR #2477.

### Tasks & Changes

#### Gallery filter API bug fix for `groupBy='all'`
- **Repo:** `audit-admin`
- **What was done:** Fixed the gallery filter API request construction so it no longer sends the search parameter when grouping by `all`, preventing incorrect filtering behavior.
- **Commits:**
  - `ad5b22ff` — [FIX][ISSUE-29] fix: Remove search param from gallery filter API when groupBy is 'all' (#2477)
- **PR:** [#2477 — [FIX][ISSUE-29] fix: Remove search param from gallery filter API when groupBy is 'all'](https://github.com/Nimbly-Technologies/audit-admin/pull/2477)