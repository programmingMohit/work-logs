## Work Log — 2026-09-17

> +0 / -0 lines

### Summary
No commits landed today — the bulk of the day went into a long hands-on validation session for the durable-submit-outbox work (Fizzy card 1776) on `audit-lite`, reproducing the missing-attachment failure path on a physical Android device. Supporting time went to figuring out network-throttling tooling and getting the physical-device dev setup working correctly. Afternoon was partly consumed by the Punthai weekly and the Stability stand-up.

### Tasks & Changes
No commits or PRs were produced today; all work was experimental/device-validation and is captured below.

### Meetings
- **Nimbly x Punthai: Weekly Meeting** — 02:30 PM–03:30 PM (60 min)
- **Stability Stand-up** — 04:00 PM–04:30 PM (30 min)
- **Ryu on Leave** — all day (no time set)

### Investigation & Research

#### Durable submit outbox — reproducing the missing-attachment failure screen (Fizzy card 1776)
- **Repo:** `audit-lite`
- **Question:** Can the missing-attachment modal and the downstream submit-failure screen be reproduced on a real device on the new durable-submit-outbox path, by deleting an attached photo just before submitting? And how do you reliably degrade the network without killing the debug build's Metro connection?
- **Outcome:** Confirmed PostHog is serving `durable-submit-outbox: true` to the test user, so the device was genuinely on the new path. Ruled out airplane mode and cable-pull approaches since both drop the adb/Metro connection for a debug build; settled on a Proxyman breakpoint to hold/fail the upload instead of a bandwidth proxy. Verified the first half of the experiment: with a photo deleted before submit, tapping **Review** on the new path does surface the missing-attachment modal as intended. `upload_jobs` state was inspected directly to confirm job bookkeeping. Submit-side behaviour was still being exercised at session end.
- **Time:** 09:33–13:50 (4h 18m)

#### Network-throttling tooling for device testing
- **Repo:** `Nimbly` (docs)
- **Question:** Where is the existing internal primer for throttling network traffic with Charles/Proxyman, and is there a FOSS equivalent worth using instead?
- **Outcome:** Located the existing throttling guide in `docs` and evaluated open-source alternatives to Charles/Proxyman; fed directly into choosing Proxyman breakpoints for the card 1776 experiment above.
- **Time:** 09:34–09:58 (25 min)

#### audit-lite physical-device dev server setup
- **Repo:** `audit-lite`
- **Question:** With a debug build already installed on the phone, is a re-run of `pnpm watch:android:prod --device` needed just to start the dev server — and why does `pnpm start` + `a` launch the emulator instead of targeting the connected phone?
- **Outcome:** Established that the install step doesn't need repeating for a server-only restart, and identified why the `a` shortcut defaults to the emulator rather than the physical device, along with the correct way to target the phone.
- **Time:** 16:08–16:18 (11 min)

### Notes
- Card 1776 experiment is **half complete**: the Review → missing-attachment-modal leg is verified; the submit-with-degraded-network leg (Proxyman breakpoint → failure screen) was still in progress at 13:50 and needs to be picked back up.
- Proxyman breakpoint is the chosen mechanism for forcing the upload failure — airplane mode and unplugging are off the table for debug builds.

---

## Work Log — 2026-09-17

> 01:32 pm – 01:32 pm · +372 / -76 lines · `audit-lite` (fix/outbox-bugfixes)

### Summary
Spent the day validating the durable submit outbox (ADR-078) against a real device rather than the emulator, using Proxyman breakpoints to force upload failures and inspect the resulting failure states. The field session surfaced several outbox bugs, which were fixed and pushed as a single change to `fix/outbox-bugfixes` and raised as PR #1983. Also wrote up a structured manual test plan for the outbox and settled the long-standing question of what happens to in-flight attachment uploads when the device drops offline mid-submission.

### Tasks & Changes

#### Outbox failure-state controls on device
- **Repo:** `audit-lite` · branch: `fix/outbox-bugfixes`
- **What was done:** Repaired the submission failure state's controls so they behave correctly on a real device. Found via a sanity-build field session where attachments were deliberately deleted pre-submit and uploads were broken with a Proxyman breakpoint, then verified the missing-attachment modal correctly appears on the new review path. Touched the start-report feature and its lock test.
- **Commits:**
  - `f539b67e` — fix(submission): repair the failed state's controls against the device
- **PR:** [#1983 — fix: outbox bugfixes from a sanity-build field session](https://github.com/Nimbly-Technologies/audit-lite/pull/1983)

### Meetings
- **Ryu on Leave** — all day
- **Nimbly x Punthai: Weekly Meeting** — 02:30 PM–03:30 PM (60 min)
- **Stability Stand-up** — 04:00 PM–04:30 PM (30 min)

### Investigation & Research

#### Offline / backgrounded behaviour of in-flight attachment uploads
- **Repo:** `audit-lite`
- **Question:** When a report is mid-upload (e.g. 5 of 10 attachments done) and the device goes offline or is backgrounded, does the attachment upload pause or fail? Does the file return to "waiting to send", does progress reset to 0%, and does the whole report upload stall?
- **Outcome:** Established that in-flight attachment uploads pause rather than fail, and resume automatically once connectivity returns — progress is not lost. Wrote this up as a QA-facing answer, plus a structured manual test doc (`MANUAL-TESTS-ADR-078-durable-submit-outbox.md`) covering the outbox scenarios, modelled on an existing regression suite. Also resolved device-run mechanics: with a debug build already installed, `pnpm start` was targeting the emulator instead of the physical device.
- **Time:** 16:08–22:33

#### Network throttling tooling for failure-path testing
- **Repo:** `Nimbly` (docs)
- **Question:** Where is the existing docs primer on throttling network traffic with Charles/Proxyman, and is there a FOSS equivalent?
- **Outcome:** Located the existing throttling primer and evaluated open-source alternatives; settled on driving the experiment with a Proxyman breakpoint rather than airplane mode, since airplane mode kills the adb/Metro connection needed to observe the failure state.
- **Time:** 09:34–09:58

#### Worklog tooling reporting zero commits
- **Repo:** `worklog`
- **Question:** Why did the worklog command report 0 git commits and miss the GitHub push, despite both having happened that day?
- **Outcome:** Root-caused to the collection layer rather than the data — repo discovery and the GitHub event source were not picking up the day's activity; fix applied so commits and pushes are captured going forward.
- **Time:** 23:35–23:43

### Notes
- PR #1983 is open and awaiting review.
- Manual test plan for ADR-078 is written but the tests themselves have not all been executed yet — the field session covered the missing-attachment modal and failure-state path only.