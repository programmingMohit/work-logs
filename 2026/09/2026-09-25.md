## Work Log — 2026-09-25

> 11:34 am – 01:13 pm · +2787 / -425 lines · `audit-lite` (fix/submission-zero-attachment-progress)

### Summary
Two report-sync PRs landed in `audit-lite`. #2040 has the outbox and submission-screen fixes that came out of the offline field sessions. #2046 adds outbox observability (tracing spans, Sentry errors and ramp counters). A follow-up fix, PR #2056, stops the submission screen showing attachment progress, and a "0 file(s) still need sending" failure headline, for reports that have no attachments. A layman's PostHog feature-flag guide for QA was also drafted.

### Tasks & Changes

#### Report-sync outbox & submission-screen bugfixes (offline field sessions)
- **Repo:** `audit-lite` · branch: `fix/submission-zero-attachment-progress`
- **What was done:** Fixed the outbox and submission-screen bugs found during the offline field testing sessions, and merged the fixes via PR.
- **Commits:**
  - `b897f7fa` — fix(report-sync): outbox and submission-screen bugfixes from the offline field sessions (#2040)
- **PR:** [#2040 — fix(report-sync): outbox and submission-screen bugfixes from the offline field sessions](https://github.com/Nimbly-Technologies/audit-lite/pull/2040)

#### Report-sync outbox observability
- **Repo:** `audit-lite` · branch: `fix/submission-zero-attachment-progress`
- **What was done:** Added observability across the outbox. Tracing spans cover the report-sync journey end to end, Sentry errors are raised for real defects, and counters track the rollout ramp.
- **Commits:**
  - `e7b4a88e` — feat(report-sync): outbox observability — spans for the journey, Sentry errors for defects, counters for the ramp (#2046)
- **PR:** [#2046 — feat(report-sync): outbox observability — spans for the journey, Sentry errors for defects, counters for the ramp](https://github.com/Nimbly-Technologies/audit-lite/pull/2046)

#### Zero-attachment progress on the submission screen
- **Repo:** `audit-lite` · branch: `fix/submission-zero-attachment-progress`
- **What was done:** The count wasn't wrong: `totalAttachments = 0` is correct for these reports. The problem was that progress UI kept rendering for a count that no longer meant anything. `SegmentedProgress` already hides itself at zero, so the fix targeted the other surfaces that don't. A second instance of the same bug was also fixed: when a report with no attachments failed, the headline read "0 file(s) still need sending." Docs were updated to match.
- **Commits:**
  - `8f52d91c` — fix: stop the submission screen reporting attachment progress a report has none of
- **PR:** [#2056 — fix: stop the submission screen reporting attachment progress a report has none of](https://github.com/Nimbly-Technologies/audit-lite/pull/2056)

### Meetings
- **Weekly Check-out** — 03:00 PM – 03:45 PM (45 min)
- **Andi Muhamad Primabudi on Leave** — all day
- **Mohit Chakraborty on Leave** — all day

### Investigation & Research

#### PostHog feature-flag guide for QA
- **Repo:** `audit-lite` (docs saved to the `Nimbly/docs` workspace)
- **Question:** How can QA enable or disable PostHog feature flags and add targeting conditions on their own, without engineering help?
- **Outcome:** Wrote a layman's guide at `~/Desktop/Nimbly/docs/guides/posthog-feature-flags-qa-guide.md`. It was then reformatted based on the inline "Notes for Claude" review comments. Each screenshot has a described placeholder for now, since the actual images haven't been added yet. The file isn't committed yet.
- **Time:** 12:02–14:24

#### Android device debugging over Wi-Fi
- **Repo:** — (local environment)
- **Question:** Why did `adb connect 192.168.29.16:5555` fail with "No route to host", even though the terminal's network permission was already on? Also, is there a way to stop having to re-enable Developer Options by tapping Build Number every time?
- **Outcome:** Troubleshot the connection with a raw TCP test from Node against port 5555, then looked at ways to keep Developer Options or wireless debugging on for longer. The sessions don't show a confirmed fix.
- **Time:** 14:15–14:35

### Notes
- The zero-attachment fix was worked against Fizzy card 1776, but the commit message doesn't reference it. Link the card on PR #2056 before merge.
- PR #2056 is open and awaiting review.
- The PostHog QA guide still needs real screenshots in place of the placeholders, and it still needs to be committed.
- Calendar marks the day as leave, but work was logged 11:34 am–2:35 pm.