## Weekly Summary — Week ending 2026-09-18

### Highlights
- **Durable submit outbox (ADR-078, Fizzy card 1776) tested on a real device:** Used Proxyman breakpoints to force upload failures and confirmed the missing-attachment modal appears on the new review path. The bugs this found were fixed in `f539b67e` and raised as [PR #1983](https://github.com/Nimbly-Technologies/audit-lite/pull/1983) (+372/-76).
- **Report-submission redesign:** Designed a new payoff screen that sits between Submit and the confetti. Rebuilt the live-uploading state (`submission-uploading-card.tsx`, `attachment-type-summary.tsx`) and synced the final designs to `dev.skynet.nimbly/audit-lite-submission-final/`.
- **Offline upload behaviour confirmed for QA:** When the device goes offline, in-flight attachment uploads pause instead of failing. They resume when the connection returns, and progress is kept. Also wrote `MANUAL-TESTS-ADR-078-durable-submit-outbox.md`.
- **ADR-078 discovery docs and product scoping:** Documented the missing-attachment modal flow and the checkout-time minting on submit. Scoped the 4-day submission-upload target list for product, including 3 design options for the uploading state.
- **Worklog tooling:** Set up an unattended 17:00 cron that sends notifications. Traced the gcalcli expiry to the OAuth app being in *Testing* status and moved it to the personal account. Built the `ytb` standup generator and fixed commit/push collection.

### By Repository
- **`audit-lite`:** Outbox failed-state fixes from the device session (PR #1983). Payoff screen and live-uploading state redesign. Untangled the conflicts in the #1982 → #1983 → #1985 stack. Wrote ADR-078 discovery docs and the manual test plan. Sorted out Android device and dev-server setup. 1 commit, +372/-76 lines. Two pushes on 09-16 have no commit data.
- **`compass`:** Reviewed PR #109. 0 commits.
- **`worklog`:** Unattended scheduler, fix for the non-expiring calendar auth, `ytb.mjs` standup generator (not committed yet), and a fix for the commit/push collection layer. 0 commits recorded.
- **`Nimbly` (docs):** Found the existing network-throttling guide and evaluated FOSS alternatives to Charles and Proxyman. 0 commits.

### Stats
- Days worked: 3 (Mon 09-15 to Wed 09-17; 09-16 was Malaysia Day, but you worked)
- Total commits: 1 recorded (`f539b67e`). The 2 pushes to `audit-lite` on 09-16 aren't counted.
- Total lines: +372 / -76
- PRs:
  - Authored: [#1983](https://github.com/Nimbly-Technologies/audit-lite/pull/1983)
  - Conflict work on: [#1982](https://github.com/Nimbly-Technologies/audit-lite/pull/1982), [#1985](https://github.com/Nimbly-Technologies/audit-lite/pull/1985)
  - Reviewed: `compass` #109 (the log has no URL)
  - Read for context: [#1931](https://github.com/Nimbly-Technologies/audit-lite/pull/1931)

### In Progress
- **PR stack:** #1982 → #1983 → #1985 still needs rebasing in dependency order. #1983 is open and waiting for review.
- **Device test:** The Review → missing-attachment-modal part passed. The submit-under-a-degraded-network part (Proxyman breakpoint → failure screen) still needs doing.
- **ADR-078:** Most manual tests haven't been run yet, and the discovery docs don't cover the whole flow yet.
- **Product decision pending:** The 3 uploading-state design options are waiting on product. The force-submit button and the missing-attachment modal changes are on hold until they decide.
- **"Failed — issues" sheet:** The frame isn't built yet. It still needs to reuse or remove the unused `AttachmentTile` in `attachment-icon.tsx`.
- **`worklog`:** `ytb.mjs` and the collection fix aren't committed yet.

---

A few things in the logs don't line up:
- **09-17 has two logs.** The first shows +0/-0. The second, generated after the collection fix, shows `f539b67e`. I used the second.
- **PR #1983 is described two ways.** On 09-15 it's a stacked PR based on `feat/submission-notifications`. On 09-17 it's a new PR from `fix/outbox-bugfixes`. Check which one is right before you share this.
- **The 09-16 pushes have no commit data.** Now that the collection fix is in, re-running the 09-16 log should pick them up and correct the commit and line totals.