## Weekly Summary — Week ending 2026-08-14

### Highlights
- Hardened the `audit-lite` background upload pipeline against offline failure modes — stopped offline signed-URL failures being recorded as permanent (#1876) and fixed the park/redrive deadlock (#1877), the two halves of a single lost-upload bug.
- Shipped `HOME_CARD_INFO_PILLS` end to end: defined the add-on in `nimbly-common` (v1.95.70) and gated the home nav card info pills behind it in `audit-lite` (#1870).
- Cleared a backlog of upload reliability fixes early in the week — orphaned worktree handling (#1860), backgrounding-aware upload timeouts (#1851), and Sentry noise from client-blocked requests (#1850).
- Home/overview UX and analytics pass: uniform rounded-box header action buttons (#1871), PostHog page-visit tracking on overview (#1872), and clearer unsynced-work auth dialog copy (#1874).
- Opened ADR-054 Phase 2 — background task native surface (#1883), the week's largest in-flight piece.

### By Repository
- **`audit-lite`:** Background upload offline resilience, home/overview UI and analytics, auth dialog copy, plus ADR-054 Phase 2 native surface work. 5 commits + 3 unattributed pushes, ~+1128/-128 lines.
- **`nimbly-common`:** Added the `HOME_CARD_INFO_PILLS` add-on to shared organization feature definitions; released v1.95.70. 1 commit.

### Stats
- Days worked: 4
- Total commits: 6 (plus 3 pushes on 08-13 with no commit metadata captured)
- Total lines: +1128 / -128
- PRs: [#1850](https://github.com/Nimbly-Technologies/audit-lite/pull/1850), [#1851](https://github.com/Nimbly-Technologies/audit-lite/pull/1851), [#1860](https://github.com/Nimbly-Technologies/audit-lite/pull/1860), [#1870](https://github.com/Nimbly-Technologies/audit-lite/pull/1870), [#1871](https://github.com/Nimbly-Technologies/audit-lite/pull/1871), [#1872](https://github.com/Nimbly-Technologies/audit-lite/pull/1872), [#1874](https://github.com/Nimbly-Technologies/audit-lite/pull/1874), [#1876](https://github.com/Nimbly-Technologies/audit-lite/pull/1876), [#1877](https://github.com/Nimbly-Technologies/audit-lite/pull/1877), [#1883](https://github.com/Nimbly-Technologies/audit-lite/pull/1883)

### In Progress
- **[#1883](https://github.com/Nimbly-Technologies/audit-lite/pull/1883) — ADR-054 Phase 2: background task native surface** (`audit-lite`, branch `feat/adr-054-phase-2-background-task-native-surface`). Open, assigned, and labeled as of 08-13; awaiting review/merge.
- Suggested follow-up from 08-12: verify #1876 and #1877 interact correctly under sustained offline conditions, since both mutate the same upload state machine.

Two data caveats worth knowing before this goes anywhere official: the 08-10 and 08-13 line counts are `+0/-0` because only GitHub event data was available those days — the real diff for #1883's three pushes is unmeasured, so the +1128/-128 total is a floor, not an actual. And #1877 is merged in GitHub but has no local commit, so the commit count of 6 undercounts merged work.

`★ Insight ─────────────────────────────────────`
- The week has a clear narrative arc that a raw commit list would hide: seven of ten PRs touch the same subsystem (background upload reliability), moving from symptom-level fixes (timeouts, Sentry noise) to root-cause state machine fixes (misclassification, deadlock) to architecture (ADR-054 Phase 2). That progression is the story worth telling in a status update.
- Line-count stats from `git log` on a single branch systematically undercount squash-merged work — the merge commit lands on `master` with a date and author that may fall outside the scan window. Building weekly stats from the GitHub PR API (`gh pr list --search "merged:2026-08-08..2026-08-14"`) gives a complete picture that local scans miss.
`─────────────────────────────────────────────────`