## Work Log — 2026-08-04

> 01:36 pm – 01:48 pm · +1001 / -138 lines · `nimbly-common` (feat/shared-password-policy-validation), `audit-admin` (fix/password-policy-mongo-source-of-truth)

### Summary
Consolidated password policy handling across the stack: extracted validation logic into `nimbly-common` as a single source of truth, and reworked `audit-admin` to read/write `passwordConfiguration` directly from Mongo instead of relying on a divergent local definition. The shared library change shipped end-to-end — PR #637 opened, merged, released, and the feature branch cleaned up — while the `audit-admin` counterpart (PR #3101) is open and pending. Additional pushes landed in `api-organizations` and `audit-lite` alongside this work.

### Tasks & Changes

#### Shared password policy validation in nimbly-common
- **Repo:** `nimbly-common` · branch: `feat/shared-password-policy-validation`
- **What was done:** Introduced a single, shared implementation of password policy validation so all consumers validate against the same rules rather than maintaining per-service copies. Change was merged and cut as release v1.95.66; the feature branch was deleted post-merge.
- **Commits:**
  - `995ae0ae` — feat(password): single source for password policy validation (v1.95.66)
- **PR:** [#637 — feat/shared-password-policy-validation](https://github.com/Nimbly-Technologies/nimbly-common/pull/637)

#### Password policy as Mongo source of truth in audit-admin
- **Repo:** `audit-admin` · branch: `fix/password-policy-mongo-source-of-truth`
- **What was done:** Reworked the admin app to read and persist `passwordConfiguration` from Mongo, removing the stale/duplicated policy state and aligning the admin surface with the shared validation from `nimbly-common`. Largest change of the day at +870 / -137 lines.
- **Commits:**
  - `74d01a41` — fix(password-policy): read and write passwordConfiguration from Mongo
- **PR:** [#3101 — fix/password-policy-mongo-source-of-truth](https://github.com/Nimbly-Technologies/audit-admin/pull/3101)

### Notes
- `audit-admin` PR #3101 is still open — awaiting review/merge. Once merged it should consume the freshly released `nimbly-common` v1.95.66.
- Pushes to `api-organizations` (two) and `audit-lite` (two) occurred the same day without local commits captured — likely related downstream consumers of the password policy change worth confirming.