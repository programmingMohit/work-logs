## Work Log — 2026-08-12

> 02:38 pm – 02:38 pm · +964 / -77 lines · `audit-lite` (master)

### Summary
Focused day on offline resilience in the background upload pipeline for `audit-lite`. Two fixes were branched, reviewed, and merged: one correcting misclassification of offline signed-URL failures as permanent, and one resolving a deadlock in the parked-upload redrive path. Both feature branches were cleaned up after merge.

### Tasks & Changes

#### Offline signed-URL failures no longer treated as permanent
- **Repo:** `audit-lite` · branch: `master`
- **What was done:** Fixed the background upload path so that signed-URL fetch failures occurring while offline are classified as transient rather than terminal, keeping affected uploads eligible for retry instead of being permanently recorded as failed. Work was done on `fix/offline-upload-terminal-misclassification`, opened and assigned in the morning, merged and the branch deleted the same day.
- **Commits:**
  - `b4a035ee` — fix(background-upload): stop offline signed-URL failures being recorded as permanent (#1876)
- **PR:** [#1876 — fix(background-upload): stop offline signed-URL failures being recorded as permanent](https://github.com/Nimbly-Technologies/audit-lite/pull/1876)

#### Offline park/redrive deadlock fix
- **Repo:** `audit-lite` · branch: `master`
- **What was done:** Addressed a deadlock in the offline park-and-redrive flow. Branch `fix/offline-park-redrive-deadlock` was created, PR opened, iterated on across several pushes, then merged and the branch deleted.
- **PR:** [#1877](https://github.com/Nimbly-Technologies/audit-lite/pull/1877)

### Notes
- PR #1877 shows as merged in GitHub activity, but no corresponding commit metadata was captured in the local git scan — commit hash and message are unavailable for that change. Worth re-running the scan against `master` to backfill.
- Both fixes target the same subsystem (offline background upload state machine); a follow-up pass to confirm the two interact correctly under sustained offline conditions would be sensible.

`★ Insight ─────────────────────────────────────`
- The two PRs are complementary halves of one failure mode: #1876 fixes *classification* (don't mark it dead), #1877 fixes *recovery* (make sure the retry queue can actually drain). Fixing only one would leave uploads either lost or stuck.
- The missing commit for #1877 is likely a squash-merge whose commit date fell outside the local scan window or on a different ref — a common gap when logs are built from `git log` on one branch rather than from the GitHub merge API.
`─────────────────────────────────────────────────`