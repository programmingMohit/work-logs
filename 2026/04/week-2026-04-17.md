## Weekly Summary — Week ending 2026-04-17

### Highlights
- Fixed issue PDF generation end-to-end across `audit-admin` and `nimbly-go-api`, including client-timezone support so rendered dates match the client’s local context.
- Improved generated PDF quality in `nimbly-go-api` by correcting department name resolution, attachment metadata handling, thumbnail rendering, and redesigning the signature section.
- Closed production-facing fixes in `audit-admin` and `admin-lite`, including correcting PDF history filter wording, stabilising CI Yarn installation via Corepack, and fixing questionnaire bulk export filenames to use `.xlsx`.
- Updated a broad set of locale files in `admin-lite` to support questionnaire, bulk schedule, shared editor/question types, and asset tracker changes.
- Improved `audit-lite` CI reliability by changing EAS build dependency behavior to avoid skip cascades while keeping the working branch synced with `master`.

### By Repository
- **`audit-admin`:** Added client timezone to PDF generation requests with a safe `UTC` fallback, corrected the PDF history filter placeholder, and stabilised deployment workflows with Corepack-based Yarn setup, 4 commits, lines included in shared day totals (`2026-04-14`: +240/-138 with `nimbly-go-api`; `2026-04-15`: +737/-728 with `admin-lite`)
- **`nimbly-go-api`:** Rendered PDF dates in client timezone, fixed department name resolution, corrected attachment JSON tags and thumbnail limits, and redesigned the PDF signature section with review follow-up fixes, 5 commits, lines included in shared day total (`2026-04-14`: +240/-138 with `audit-admin`)
- **`admin-lite`:** Fixed questionnaire bulk export filenames to use `.xlsx`, added explicit format-to-extension mapping, and updated related locale files across multiple modules, 14 commits, lines included in shared day total (`2026-04-15`: +737/-728 with `audit-admin`)
- **`audit-lite`:** Updated EAS build dependency configuration to prevent skip cascades and merged `master` into the active branch, 2 commits, +6/-4 lines

### Stats
- Days worked: 3
- Total commits: 25
- Total lines: +983 / -870
- PRs: [#2904](https://github.com/Nimbly-Technologies/audit-admin/pull/2904), [#93](https://github.com/Nimbly-Technologies/nimbly-go-api/pull/93), [#94](https://github.com/Nimbly-Technologies/nimbly-go-api/pull/94), [#95](https://github.com/Nimbly-Technologies/nimbly-go-api/pull/95), [#96](https://github.com/Nimbly-Technologies/nimbly-go-api/pull/96), [#2907](https://github.com/Nimbly-Technologies/audit-admin/pull/2907), [#2908](https://github.com/Nimbly-Technologies/audit-admin/pull/2908), [#268](https://github.com/Nimbly-Technologies/admin-lite/pull/268)

### In Progress
Continued work in `audit-lite` on `fix/questionnaire-infinite-render-loops`; CI dependency behavior was adjusted and the branch was synced with `master`, but validation and the main questionnaire render loop fix remain unfinished.