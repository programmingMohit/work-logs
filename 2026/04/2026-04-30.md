## Work Log — 2026-04-30

> 01:21 am – 02:33 pm · +489 / -183 lines · `api-issues` (master), `nimbly-web-api` (chore/bypass-pact-gate), `audit-lite` (observability/AUDIT-LITE-V2-4KE)

### Summary
The day focused on production-facing fixes across backend services and targeted observability work in Audit Lite. Work included improving Firebase read batching, correcting report download timeout handling, repairing a corrupted lockfile entry, and adding instrumentation to diagnose a stale `TextInput` crash. A temporary pact gate bypass was also introduced to unblock progress while a contract mismatch is resolved.

### Tasks & Changes

#### Batch Firebase reads and update entity-node
- **Repo:** `api-issues` · branch: `master`
- **What was done:** Improved `populateReadStatus` by batching Firebase reads and updated `entity-node`, reducing inefficient read behavior while keeping dependencies current.
- **Commits:**
  - `7422ce45` — fix: batch Firebase reads in populateReadStatus + bump entity-node to 1.70.34 (#396)
- **PR:** [#396 — fix: batch Firebase reads in populateReadStatus + bump entity-node to 1.70.34](https://github.com/Nimbly-Technologies/api-issues/pull/396)

#### Restore corrupted gaxios lockfile entry
- **Repo:** `api-issues` · branch: `master`
- **What was done:** Repaired a corrupted `gaxios` entry in `package-lock.json` to stabilize dependency resolution and keep the branch in a clean, installable state.
- **Commits:**
  - `e6a6e1f1` — fix(lockfile): restore corrupted gaxios entry in package-lock.json (#397)
- **PR:** [#397 — fix(lockfile): restore corrupted gaxios entry in package-lock.json](https://github.com/Nimbly-Technologies/api-issues/pull/397)

#### Mark timed out report downloads as failed
- **Repo:** `nimbly-web-api` · branch: `chore/bypass-pact-gate`
- **What was done:** Updated report download handling so timed out jobs are explicitly marked as failed, improving downstream status accuracy and operational visibility.
- **Commits:**
  - `a87fbca4` — fix: mark timed out report downloads as failed (#384)
- **PR:** [#384 — fix: mark timed out report downloads as failed](https://github.com/Nimbly-Technologies/nimbly-web-api/pull/384)

#### Temporarily bypass pact gate for contract mismatch
- **Repo:** `nimbly-web-api` · branch: `chore/bypass-pact-gate`
- **What was done:** Added a temporary bypass for the pact gate to unblock delivery while the `notification-events` contract mismatch is being addressed.
- **Commits:**
  - `153f8776` — chore: bypass pact gate temporarily (notification-events contract mismatch)

#### Add observability breadcrumbs for stale TextInput crash investigation
- **Repo:** `audit-lite` · branch: `observability/AUDIT-LITE-V2-4KE`
- **What was done:** Added focus and blur breadcrumbs, extracted questionnaire input tracking into a dedicated hook, and cleaned up related typing and component structure to improve crash diagnosis around stale `TextInput` behavior.
- **Commits:**
  - `84b767eb` — observability(AUDIT-LITE-V2-4KE): add focus/blur breadcrumbs to surface stale TextInput crash
  - `b33acb10` — refactor(AUDIT-LITE-V2-4KE): extract useQuestionnaireInputTracking hook; log breadcrumb before onBlur
  - `752cd3cb` — fix(AUDIT-LITE-V2-4KE): fix indentation in search-bar; add QuestionInputType union type to hook
  - `6db14a96` — fix(AUDIT-LITE-V2-4KE): fix Search.Input indentation; extract SearchBarProps interface

### Notes
The pact gate bypass in `nimbly-web-api` is explicitly temporary and should be removed once the `notification-events` contract mismatch is fixed. The Audit Lite observability work appears to be in progress toward isolating the stale `TextInput` crash rather than closing the issue outright.