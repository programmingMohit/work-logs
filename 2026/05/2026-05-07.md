## Work Log — 2026-05-07

> 12:38 am – 04:37 pm · +633 / -617 lines · `audit-lite` (fix/eas-build-view-working-directory)

### Summary
Focused on stabilising `audit-lite`'s Maestro and Android CI pipeline, with repeated fixes to emulator readiness, checkout/runtime setup, and parallel job isolation. The work progressed from diagnostics and script extraction into full ADB server isolation and direct `dadb` connection fixes to make parallel EAS build validation more reliable.

### Tasks & Changes

#### Maestro CI stabilisation and ADB isolation
- **Repo:** `audit-lite` · branch: `fix/eas-build-view-working-directory`
- **What was done:** Hardened the GitHub Actions Maestro test pipeline by adding failure diagnostics, extracting oversized inline scripts, fixing missing runner checkout state, improving launcher and `FallbackHome` recovery, scoping temp files per run, resetting emulator state on boot, isolating AVD and ADB resources across parallel jobs, and correcting direct `dadb` connection port usage.
- **Commits:**
  - `258e5b0a` — diag(ci): add system state dumps on readiness timeout and Unable to launch + 60s recovery sleep
  - `f86dd479` — refactor(ci): extract test script to file — fix GitHub Actions 21000-char expression limit
  - `66053a4c` — fix(ci): add checkout step to test job so .github/scripts/ is available on runner
  - `dc20d31f` — fix(ci): evict FallbackHome before each test — launch Launcher explicitly and gate readiness on FallbackHome gone
  - `f0ea3cb3` — fix(ci): scope /tmp test-file paths to GITHUB_RUN_ID
  - `17e5ccb7` — fix(ci): remove pm disable-user for nexuslauncher — breaks FallbackHome recovery
  - `5ce3e4b1` — fix(ci): add -wipe-data to emulator options — reset data partition on every boot
  - `8bc5960d` — ci(maestro): isolate AVD dir and ADB server per parallel job
  - `106403d2` — ci(maestro): full ADB server isolation via ANDROID_ADB_SERVER_PORT
  - `3baec43a` — ci(maestro): add MAESTRO_ADB_PORT to point dadb at isolated ADB server
  - `4c7b761d` — ci(maestro): switch to --port ADBD_PORT for direct dadb connection
  - `43fc01f4` — ci(maestro): reduce retries from 3 attempts to 2
  - `6cf3e39a` — fix(maestro): use ADB server port for --port flag, not adbd port
  - `cce81e24` — fix(maestro): use --host localhost --port ADBD_PORT for direct dadb connection

### Notes
Work appears to still be in progress on the same branch, with several late-day follow-up commits indicating active iteration on the final `dadb`/ADB port configuration.