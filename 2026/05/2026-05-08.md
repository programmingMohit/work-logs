## Work Log — 2026-05-08

> 03:42 pm – 04:52 pm · +648 / -455 lines · `qa` (fix/login-shard-email-interpolation), `audit-lite` (ci/maestro-shard-split-rearchitecture)

### Summary
Worked across two CI-related efforts: stabilising sharded login email handling in `qa` and reworking Maestro CI orchestration in `audit-lite`. The day focused on making shard-specific configuration safer, simplifying CI execution flow, and hardening emulator/network cleanup to reduce flaky runs.

### Tasks & Changes

#### Shard-specific login email handling in CI
- **Repo:** `qa` · branch: `fix/login-shard-email-interpolation`
- **What was done:** Updated the login flow and CI configuration so shard-indexed emails are resolved safely in `evalScript`, avoiding brittle nested variable access and improving reliability for `--shard-split` runs.
- **Commits:**
  - `0022f898` — ci(login): use shard-indexed email for --shard-split CI (#55)
  - `d8fa6040` — fix(login): resolve shard email via evalScript instead of nested vars
  - `4722fdf5` — fix(login): use this.VAR to safely access optional env vars in evalScript
  - `e547803e` — fix(login): harden evalScript email resolution
- **PR:** [#55 — ci(login): use shard-indexed email for --shard-split CI](https://github.com/Nimbly-Technologies/qa/pull/55)

#### Maestro CI shard-split rearchitecture
- **Repo:** `audit-lite` · branch: `ci/maestro-shard-split-rearchitecture`
- **What was done:** Reworked Maestro CI to run through a single-runner `--shard-split` approach, then iterated on artifact resolution, checkout flow, emulator port release handling, and stale netsim cleanup to reduce cross-run interference and improve observability.
- **Commits:**
  - `91dc1036` — ci(maestro): rearchitect to single runner + --shard-split
  - `f6aee7b4` — fix(ci): use Expo GraphQL API to resolve build artifact URL
  - `cbf1bb13` — fix(ci): wait for emulator ports to be released before booting new ones
  - `c90c5475` — ci(maestro): fix stale netsim + concurrent run interference
  - `b0546445` — ci(maestro): add checkout step to test job
  - `d797ec6d` — ci(maestro): add diagnostic logging to catch emulator kill source
  - `47d7b9bc` — ci(maestro): kill stale netsim with sudo + pre-boot assertion

### Notes
`audit-lite` work appears to be in progress, with multiple follow-up commits focused on CI diagnostics and emulator stability after the initial rearchitecture.