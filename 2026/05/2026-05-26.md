## Work Log — 2026-05-26

> 04:31 pm – 04:31 pm · +384 / -20 lines · `audit-lite` (fix/audit-lite-v2-57s-client-blocked-deferred-drain)

### Summary
Work focused on stabilising background upload behaviour in `audit-lite` by changing how `ClientBlockedError` cases are handled. The update defers blocked uploads instead of consuming retry budget, reducing unnecessary retry exhaustion and improving resilience in deferred drain flows.

### Tasks & Changes

#### Background upload retry handling for ClientBlockedError
- **Repo:** `audit-lite` · branch: `fix/audit-lite-v2-57s-client-blocked-deferred-drain`
- **What was done:** Updated background upload logic so `ClientBlockedError` uploads are deferred rather than counted against the retry budget, addressing blocked deferred-drain behaviour and improving failure handling in the upload pipeline.
- **Commits:**
  - `bd71a87f` — fix(background-upload): defer ClientBlockedError uploads instead of burning retry budget
- **PR:** [#1666 — fix(background-upload): defer ClientBlockedError uploads instead of burning retry budget](https://github.com/Nimbly-Technologies/audit-lite/pull/1666)