## Work Log — 2026-05-06

> 12:29 am – 04:59 pm · +267 / -208 lines · `qa` (fix/remove-unused-alert-handlers), `audit-lite` (fix/eas-build-view-working-directory)

### Summary
Spent the day hardening CI test reliability across two repos. In `qa`, stripped out alert/dialog handlers that never fire in CI and were adding ~30s per test each. In `audit-lite`, tackled a long tail of Maestro CI infrastructure issues: ADB instability, stdin consumption, notification permission suppression, hung pipes, and dex2oat mid-run interference.

### Tasks & Changes

#### Remove unused alert handlers from QA login flows
- **Repo:** `qa` · branch: `fix/remove-unused-alert-handlers`
- **What was done:** Progressively stripped Samsung stylus popup handler, Alert dialog handlers, and push notification handler from login flows. Fixed the `dismiss-alerts.yaml` path reference (was 3 levels up, incorrect) and corrected invalid timeout syntax in `login.yaml`. Restored `stopApp: true` to prevent the Multiple Device Login dialog. Final commit removes the Alert and "Got it!" handlers entirely since they never fire in CI and each cost ~30s.
- **Commits:**
  - `6805d12e` — fix(dismiss-alerts): remove Alert and Got it! handlers — never fire in CI, each costs ~30s
  - `07e1239f` — fix(dismiss-alerts): remove push notification handler (#51)
  - `60a99c9f` — fix(login): restore stopApp: true to prevent Multiple Device Login dialog (#50)
  - `c4ef9a4a` — fix(09-login-to-home): correct dismiss-alerts.yaml path — 3 levels up was wrong
  - `492c44d7` — fix(reusable-flows): fix login.yaml path + remove invalid timeout syntax (#48)
  - `df551264` — fix(login): dismiss Push Notifications alert on emulator with short timeout (#45)
  - `a57276b0` — fix(login): remove Alert dialog handlers (#43)
  - `7c8ebf9a` — fix(login): remove Samsung stylus popup handler (#41)
- **PRs:**
  - [#51 — fix(dismiss-alerts): remove push notification handler](https://github.com/Nimbly-Technologies/qa/pull/51)
  - [#50 — fix(login): restore stopApp: true to prevent Multiple Device Login dialog](https://github.com/Nimbly-Technologies/qa/pull/50)
  - [#48 — fix(reusable-flows): fix login.yaml path + remove invalid timeout syntax](https://github.com/Nimbly-Technologies/qa/pull/48)
  - [#45 — fix(login): dismiss Push Notifications alert on emulator with short timeout](https://github.com/Nimbly-Technologies/qa/pull/45)
  - [#43 — fix(login): remove Alert dialog handlers](https://github.com/Nimbly-Technologies/qa/pull/43)
  - [#41 — fix(login): remove Samsung stylus popup handler](https://github.com/Nimbly-Technologies/qa/pull/41)

---

#### Stabilise Maestro CI pipeline in audit-lite
- **Repo:** `audit-lite` · branch: `fix/eas-build-view-working-directory`
- **What was done:** Comprehensive CI hardening pass. Fixed POST_NOTIFICATIONS suppression (appops deny → user-fixed flag → USER_SET flag, re-applied before each test). Resolved ADB instability: restarted server before Maestro to flush stale offline entries, added reconnect-offline handling, and recovery logic for inter-test device disconnects. Fixed stdin being consumed by `adb shell` by using fd 3 for the test loop. Pre-created Maestro log dir to prevent `NoSuchFileException` crash; later taught the pipeline to treat `NoSuchFileException` (and gRPC UNAVAILABLE) as infrastructure failures rather than test failures. Added Maestro timeout to prevent hung pipes, wait-for-system-ready before each test to handle mid-run dex2oat, and fixed report summary always showing 0/0. Guarded emulator alert behind `__DEV__` flag.
- **Commits:**
  - `3646da35` — fix(notifications): guard emulator alert behind __DEV__ flag
  - `60d14e4b` — fix(ci): treat gRPC UNAVAILABLE as infrastructure failure in NoSuchFileException bypass
  - `0a32eabe` — fix(ci): add USER_SET flag to POST_NOTIFICATIONS suppression
  - `51f6cda9` — fix(ci): re-apply POST_NOTIFICATIONS user-fixed flag before each test
  - `02cfc199` — fix(ci): timeout Maestro to prevent hung pipe + handle NoSuchFileException as pass
  - `57f0ce0b` — fix(ci): wait for system ready before each test to handle mid-run dex2oat
  - `e84f3980` — fix(ci): use fd 3 for test loop to prevent adb shell eating stdin
  - `107f4966` — fix(ci): recover from inter-test device disconnect + add diagnostics
  - `5c9aa660` — fix(ci): fix report summary always showing 0/0
  - `c4518986` — fix(ci): use reconnect offline + fix retry list self-delete
  - `7a83d845` — fix(ci): pre-create Maestro log dir to prevent NoSuchFileException crash
  - `66e5c840` — fix(ci): restart ADB server before Maestro to flush stale offline entries
  - `ac58776f` — ci(maestro): run all tests, only retry infrastructure failures
  - `07998ff2` — ci(maestro): fix POST_NOTIFICATIONS suppression and skip retry on assertion failures
  - `f12ceb25` — ci(maestro): re-enable disable-animations: true
  - `7495d415` — ci(maestro): use appops POST_NOTIFICATION deny to suppress dialog on cold start
  - `84eb8109` — ci(maestro): use user-fixed flag to permanently deny POST_NOTIFICATIONS

### Notes
- `qa` PR #52 was opened but no corresponding commit is present in today's data — likely in-flight or targeting cleanup beyond what's in this log.
- `audit-lite` PR #1611 was labeled today; the full CI hardening work on `fix/eas-build-view-working-directory` appears to be ongoing with no merge yet.