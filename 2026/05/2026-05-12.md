## Work Log — 2026-05-12

> 12:50 am – 03:55 pm · +22 / -7 lines · `admin-lite` (master), `api-schedules` (fix/strip-schedule-active-period-for-adhoc-only)

### Summary
Focused on two bug fixes across `api-schedules` and `admin-lite`. The day started with a backend safeguard for adhoc-only schedule upserts, then shifted to a frontend fix preventing radio button state from resetting in the assignment modal. Both changes were merged by the end of the day.

### Tasks & Changes

#### Prevent radio button reset in assignment modal
- **Repo:** `admin-lite` · branch: `master`
- **What was done:** Fixed the questionnaire editor assignment modal so radio button selections no longer reset unexpectedly, improving form state stability during assignment flows.
- **Commits:**
  - `cb3b7b1c` — fix(questionnaire-editor): prevent radio button reset in assignment modal (#286)
- **PR:** [#286 — fix(questionnaire-editor): prevent radio button reset in assignment modal](https://github.com/Nimbly-Technologies/admin-lite/pull/286)

#### Strip `scheduleActivePeriod` for adhoc-only schedule upserts
- **Repo:** `api-schedules` · branch: `fix/strip-schedule-active-period-for-adhoc-only`
- **What was done:** Updated schedule site upsert handling to remove `scheduleActivePeriod` when the schedule is adhoc-only, preventing invalid or unnecessary active period data from being persisted.
- **Commits:**
  - `faeed51e` — fix(scheduleSite): strip scheduleActivePeriod for adhoc-only schedules on upsert
- **PR:** [#387 — fix(scheduleSite): strip scheduleActivePeriod for adhoc-only schedules on upsert](https://github.com/Nimbly-Technologies/api-schedules/pull/387)