## Work Log — 2026-05-13

> 10:17 am – 03:20 pm · +1245 / -2285 lines · `admin-lite` (fix/interleaved-category-duplicate-keys), `api-user-roles` (fix/preserve-null-origin-hierarchy-validation)

### Summary
Most of the day focused on stabilising `admin-lite` questionnaire/category handling for interleaved blocks, especially around duplicate keys, canonical category assignment, drag-and-drop preview consistency, and payload validation edge cases. Translation updates for questionnaire templates were also committed in the same branch, and a separate `api-user-roles` fix addressed preserving `null` origin metadata for base roles. Both branches were opened as PRs and merged the same day.

### Tasks & Changes

#### Interleaved category assignment and duplicate key fixes
- **Repo:** `admin-lite` · branch: `fix/interleaved-category-duplicate-keys`
- **What was done:** Fixed category assignment resolution for interleaved questionnaire blocks, stabilised composite keys used across the editor and preview flows, aligned sortable IDs for drag-and-drop, and tightened ObjectId validation logic to avoid edge-case failures.
- **Commits:**
  - `166015f9` — fix(assignment-service): resolve category assignment lookup for interleaved blocks
  - `78c1f28a` — fix(category-manager): write assignment under canonical Mongo ID for interleaved blocks
  - `ea5797c7` — fix(category-manager): move canonicalCategoryId before first use
  - `e37d8f68` — fix(mini-preview): align useSortable id with SortableContext composite key
  - `196a879f` — fix(questionnaire-editor): safe composite key separator and simplified dedup loop
  - `2e3ae08e` — fix(payload-transformer): make MongoDB ObjectId regex case-insensitive
  - `7397b71a` — fix(preview-questionnaire-modal): remove unused categoryIndex after key simplification
  - `5d70d498` — fix(questionnaire-editor): consistent split limit and stable formula-condition keys
  - `e8f27b42` — refactor(mini-preview): replace index parsing with findIndex/find for dnd lookups
- **PR:** [#287 — Fix interleaved category duplicate keys](https://github.com/Nimbly-Technologies/admin-lite/pull/287)

#### Questionnaire template locale updates
- **Repo:** `admin-lite` · branch: `fix/interleaved-category-duplicate-keys`
- **What was done:** Updated questionnaire and shared template translations, with several follow-up locale commits to finalise the language resource changes.
- **Commits:**
  - `be76fc25` — Update translations for questionnaire templates and shared templates
  - `d6be7326` — locale
  - `5f91f36f` — locale
  - `92ae632c` — chore: locale
  - `9e863a12` — chore: locale
  - `cb982264` — chore: locale
- **PR:** [#287 — Fix interleaved category duplicate keys](https://github.com/Nimbly-Technologies/admin-lite/pull/287)

#### Preserve null origin metadata for base roles
- **Repo:** `api-user-roles` · branch: `fix/preserve-null-origin-hierarchy-validation`
- **What was done:** Corrected role metadata editing so base roles retain a `null` origin instead of being normalised incorrectly, preserving expected hierarchy validation behavior.
- **Commits:**
  - `84eb1d12` — fix(user-roles): preserve null origin for base roles in editRoleMetadata
- **PR:** [#57 — Fix preserve null origin hierarchy validation](https://github.com/Nimbly-Technologies/api-user-roles/pull/57)