## Work Log — 2026-05-05

> 12:24 pm – 04:59 pm · +469 / -135 lines · `audit-lite` (fix/eas-build-view-working-directory), `audit-admin` (fix/bulk-block-reason-modal)

### Summary
Spent the bulk of the day hardening the EAS CI pipeline in `audit-lite` — iteratively fixing emulator lifecycle management, ADB shim installation, QEMU stale-process detection, and build artifact URL resolution across 17 commits. Wrapped up with a quick two-commit fix in `audit-admin` to surface the blocked reason modal correctly on bulk status changes and localize the error string.

### Tasks & Changes

#### EAS CI Pipeline — Emulator & Build Artifact Fixes
- **Repo:** `audit-lite` · branch: `fix/eas-build-view-working-directory`
- **What was done:** Systematically resolved a cascade of CI failures: stale emulator cleanup (lock files, QEMU age-checking, ADB daemon restarts), ADB shim installation race conditions (ETXTBSY, heredoc YAML indentation), emulator boot retry logic, Maestro pre-test diagnostics, and build artifact URL resolution via the Expo GraphQL API instead of `eas build:view`.
- **Commits:**
  - `a92b1b8e` — fix(ci): run eas build:view from apps/expo to resolve build URL
  - `dfad61c7` — fix(ci): surface eas build:view errors for debugging
  - `e07501c0` — fix(ci): use Expo GraphQL API to resolve build artifact URL
  - `2c7150e7` — fix(ci): kill stale emulators before starting test job
  - `f8c5aca7` — fix(ci): clean AVD lock files after killing stale emulators
  - `dfd30969` — fix(ci): restart ADB daemon after killing stale emulators
  - `600c6797` — fix(ci): move stale emulator cleanup to after Android SDK setup
  - `886f110f` — fix(ci): remove adb kill-server from stale emulator cleanup
  - `02466917` — fix(ci): only kill stale QEMU processes older than 5 minutes
  - `f50f9724` — fix(ci): fix QEMU age-check parsing and add watchdog logging
  - `ab38521d` — fix(ci): fix watchdog QEMU pid lookup and improve death logging
  - `2242c9cc` — feat(ci): add 2-retry mechanism for flaky emulator boot failures
  - `89e74fae` — fix(ci): install ADB shim to route adb -e to specific emulator
  - `5320111c` — fix(ci): fix ADB shim heredoc — use printf to avoid YAML indentation issue
  - `6f3ee92f` — fix(ci): fix ADB shim install failing when stale shim cleanup deleted adb binary
  - `38455640` — fix(ci): fix ETXTBSY when installing ADB shim over running daemon binary
  - `36df2b4d` — debug(ci): add ADB diagnostics before Maestro test loop
- **PR:** [#1611 — fix(ci): run eas build:view from apps/expo to resolve build URL](https://github.com/Nimbly-Technologies/audit-lite/pull/1611)

---

#### Bulk Block Reason Modal — Bug Fix & i18n
- **Repo:** `audit-admin` · branch: `fix/bulk-block-reason-modal`
- **What was done:** Fixed the bulk status change flow to correctly show the blocked reason modal when needed, and localized the blocked reason error string in `BulkBlockedModal`.
- **Commits:**
  - `e4fc6ee5` — fix(bulk-edit): show blocked reason modal on bulk status change
  - `e91991e1` — i18n(bulk-edit): localize blocked reason error string in BulkBlockedModal
- **PR:** [#2958](https://github.com/Nimbly-Technologies/audit-admin/pull/2958)