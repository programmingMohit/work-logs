## Work Log — 2026-03-24

> 01:00 pm – 01:32 pm · +262 / -163 lines · `audit-lite` (fix/ios-fcm-foreground-apns-registration)

### Summary
Spent the morning fixing iOS FCM foreground push notification reliability in `audit-lite`. The work centered on ensuring APNs token registration happens eagerly before `onMessage` subscribes, eliminating a race condition and silent failure path. Wrapped up with a merge from master and unhandled rejection hardening.

### Tasks & Changes

#### iOS FCM Foreground APNs Registration Fix
- **Repo:** `audit-lite` · branch: `fix/ios-fcm-foreground-apns-registration`
- **What was done:** Fixed a race condition where APNs token registration was deferred too late, causing silent failures when FCM's `onMessage` subscribed before APNs was ready. Also removed a stack trace leak, eliminated a duplicate test, and hardened the constructor against unhandled promise rejections.
- **Commits:**
  - `dea5ec25` — Merge branch 'master' into fix/ios-fcm-foreground-apns-registration
  - `15f8bae4` — fix(firebase-messaging): handle unhandled rejection in constructor
  - `a7e7a0ae` — fix(firebase-messaging): remove stack trace leak and duplicate test
  - `67fab9a1` — fix(firebase-messaging): fix race condition and silent APNs failure
  - `ca8e652e` — fix(firebase-messaging): eagerly register APNs before onMessage subscribes
- **PR:** [#1506](https://github.com/Nimbly-Technologies/audit-lite/pull/1506)

### Meetings

- **Architecture Planning & Discussion** · 01:00 PM – 02:20 PM (80 min)

### Notes
- PR #1506 is open but not yet merged — follow up on review.