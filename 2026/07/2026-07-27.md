`★ Insight ─────────────────────────────────────`
The activity payload had empty `commits`/`repoStats` (no local clones scanned), so the only signal was GitHub `PullRequestEvent` merges. Reconstructing the log meant pulling commits + diffstat back out of the GitHub API — worth knowing that merged-PR events alone are enough to rebuild a full day's log.
Both PRs share a branch name across two repos, which is the tell that they're one coordinated change: admin-lite writes the flag, audit-admin reads it.
`─────────────────────────────────────────────────`

## Work Log — 2026-07-27

> +28 / -10 lines

### Summary
Shipped the Executive Dashboard health-score peer-comparison removal-holdout experiment as a coordinated two-repo change. The `"X% above/below industry peers"` line is now gated behind a new org-level `HEALTH_SCORE_PEER_COMPARISON` flag defaulting to off, with a CS-facing toggle in the Feature Access screen for per-org re-enablement. Both PRs merged and their branches were cleaned up.

### Tasks & Changes

#### Health score peer-comparison removal holdout — UI gate
- **Repo:** `audit-admin` · branch: `feat/health-score-peer-comparison-holdout`
- **What was done:** Registered `HEALTH_SCORE_PEER_COMPARISON` in the `AdminLiteFeatures` union and reducer initial state (default `false`, so the fetch saga always seeds the key), then gated the peer-comparison line on the Executive Dashboard health score card behind it via `useSelector`. Also fixed a latent styling bug where the "above peers" green used a raw hex `#00AB4E` instead of the Tailwind wrapper `aa-text-[#00AB4E]`, so the color never applied. Net effect: instant per-org kill switch with no deploy.
- **Commits:**
  - `95bda19` — feat: gate Health Score peer-comparison line behind HEALTH_SCORE_PEER_COMPARISON flag
  - `27be3b1` — refactor: defensive fallback on peer-comparison flag selector
- **PR:** [#3079 — feat: gate Health Score peer-comparison line behind HEALTH_SCORE_PEER_COMPARISON flag (removal holdout)](https://github.com/Nimbly-Technologies/audit-admin/pull/3079)

#### Health score peer-comparison removal holdout — CS toggle
- **Repo:** `admin-lite` · branch: `feat/health-score-peer-comparison-holdout`
- **What was done:** Added the `HEALTH_SCORE_PEER_COMPARISON` entry to `getFeatureDefinitions()` in `app/settings/feature-access/feature-access-service.ts`, surfacing a toggle row under the "Additional" category on `/settings/feature-access` (embedded by audit-admin at `/settings/feature-access-v2`). Existing `as Feature[]` cast already permits admin-lite-only keys, so no `nimbly-common` package or type change was needed; `check-types` passes.
- **Commits:**
  - `10cc86c` — feat: add HEALTH_SCORE_PEER_COMPARISON to feature access screen
- **PR:** [#323 — feat: add HEALTH_SCORE_PEER_COMPARISON toggle to feature access screen](https://github.com/Nimbly-Technologies/admin-lite/pull/323)

### Notes
- **Known limitation:** the flag only persists for orgs on the **`custom`** package. For `starter`/`pro`/`enterprise`, the backend regenerates the features map from the package enum and discards submitted keys — forcing the peer line on for those orgs would require adding the key to the `@nimbly-technologies/nimbly-common` `FeaturesType` enum.
- Because the peer line is passive/non-interactive, success is measured by *absence of demand* (CS tickets / CSM mentions) rather than engagement — needs a follow-up check-in window and an exclusion list of orgs to re-enable.
- No `api-organizations` change was required; the `features` map round-trips unknown keys for `custom`-package orgs.