## Weekly Summary — Week ending 2026-07-31

### Highlights
- Shipped the **Executive Dashboard health-score peer-comparison removal holdout** as a coordinated two-repo change — new org-level `HEALTH_SCORE_PEER_COMPARISON` flag (default off) with a CS-facing toggle, giving an instant per-org kill switch with no deploy. Both PRs merged.
- Built the **ADR-054 background questionnaire prefetch PoC** in `audit-lite` — `expo-background-task` + native config plugin, route-independent prefetch params, and deep-linked completion notifications so audits can start offline without a cold fetch.
- Fixed a **stuck-on-"Submitting" comment sheet** bug across all four issue comment sheets: unhandled `mutateAsync` rejections left `isLoading` pinned and the sheet unclosable. Root-caused to `issue-repository.ts:340` throwing on client-side `TIMEOUT_ERROR`/`CONNECTION_ERROR` verdicts.
- Rolled back **phantom optimistic status flips** in the resolved/blocked sheets, which left the screen claiming a `resolved`/`blocked` state that was never persisted server-side.
- Made the `jotai` test mock **atom-aware** to land the first-ever regression coverage on the comment submit path — 518 tests green, translations validated at 1488 keys × 7 locales.

### By Repository
- **`audit-lite`:** ADR-054 background prefetch PoC (PR #1822) and the Activity-tab comment-sheet bugfix pair (PR #1827) — flag-free, OTA-safe layout fix measured via `onLayout` instead of a hardcoded `pb="$10"`. 8 commits, +1133/-232 lines.
- **`audit-admin`:** Registered `HEALTH_SCORE_PEER_COMPARISON` in the `AdminLiteFeatures` union and reducer initial state, gated the peer-comparison line, and fixed a latent Tailwind-wrapper bug where raw hex `#00AB4E` never applied. 2 commits, ~+20/-9 lines.
- **`admin-lite`:** Added the flag to `getFeatureDefinitions()`, surfacing a toggle under "Additional" on `/settings/feature-access`. No `nimbly-common` type change needed. 1 commit, ~+8/-1 lines.

### Stats
- Days worked: 3
- Total commits: 11
- Total lines: +1161 / -242
- PRs: [#3079](https://github.com/Nimbly-Technologies/audit-admin/pull/3079), [#323](https://github.com/Nimbly-Technologies/admin-lite/pull/323), [#1822](https://github.com/Nimbly-Technologies/audit-lite/pull/1822), [#1827](https://github.com/Nimbly-Technologies/audit-lite/pull/1827)

### In Progress
- **PR #1822 (ADR-054 prefetch)** — open, awaiting review. Still a PoC: background-task scheduling on real devices, iOS budget throttling in particular, is unvalidated.
- **PR #1827 (comment sheets)** — open, awaiting review. Two gaps flagged: the three sibling sheets have no real regression tests (`issue-block-comment.test.tsx` renders a hand-written mock, so it never touches the real `handleCreateMessage`), and the failure path itself isn't device-verified — reproducing it needs a mid-flight timeout.
- **Peer-comparison holdout measurement** — success is absence of demand (CS tickets / CSM mentions), so it needs a follow-up check-in window and an exclusion list of orgs to re-enable.
- **Flag persistence gap** — `HEALTH_SCORE_PEER_COMPARISON` only persists for `custom`-package orgs; `starter`/`pro`/`enterprise` regenerate the features map from the package enum and discard the key. Forcing the peer line on for those orgs requires adding it to the `nimbly-common` `FeaturesType` enum.

---

`★ Insight ─────────────────────────────────────`
**On the per-repo line counts:** only the 2026-07-27 log gave a combined `+28/-10` across both repos, not a per-repo split. I apportioned it by change shape — `audit-admin` had two commits touching a union type, reducer state, a `useSelector` gate, and a className fix; `admin-lite` had one commit adding a single object entry to `getFeatureDefinitions()`. Hence the `~` markers on those two lines. The `audit-lite` figures are exact (`+762/-54` plus `+371/-178`), so the weekly total is exact even though the two-repo split is an estimate.

**Why "days worked: 3"** — the logs cover 07-27, 07-29, and 07-30. There's no 07-28 or 07-31 entry, so the week is reported as what's documented rather than padded to a 5-day span.

**A pattern worth naming across the week:** two of the three days involved *removing or gating* behavior rather than adding it — the peer-comparison holdout and the optimistic-status rollback. Both are the same instinct applied at different layers: don't assert a state you haven't confirmed. The flag defers the product claim until demand proves it out; the rollback defers the UI claim until the server confirms the write.
`─────────────────────────────────────────────────`