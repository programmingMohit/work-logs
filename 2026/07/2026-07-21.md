## Work Log — 2026-07-21

> 09:57 am – 01:34 pm · +128 / -33 lines · `admin-lite` (feat/health-score-peer-comparison-holdout), `audit-lite` (fix/audit-lite-v2-5hp-questionnaire-poll-throw), `audit-admin` (feat/health-score-peer-comparison-holdout)

### Summary
Shipped a feature-flag holdout for the Health Score peer-comparison line, wiring the `HEALTH_SCORE_PEER_COMPARISON` flag across `admin-lite` and `audit-admin` with a defensive selector fallback. Hardened the `audit-lite` questionnaire polling path against nullish maps and benign no-update responses, and merged an earlier schedule site-filter debounce fix.

### Tasks & Changes

#### Health Score peer-comparison flag holdout
- **Repo:** `admin-lite` · branch: `feat/health-score-peer-comparison-holdout` / `audit-admin` · branch: `feat/health-score-peer-comparison-holdout`
- **What was done:** Gated the Health Score peer-comparison line behind the `HEALTH_SCORE_PEER_COMPARISON` flag as a removal holdout — exposed the flag in the admin feature-access screen and added a defensive fallback on the flag selector so a missing flag degrades gracefully.
- **Commits:**
  - `10cc86c2` — feat: add HEALTH_SCORE_PEER_COMPARISON to feature access screen
  - `95bda190` — feat: gate Health Score peer-comparison line behind HEALTH_SCORE_PEER_COMPARISON flag
  - `27be3b10` — refactor: defensive fallback on peer-comparison flag selector
- **PR:** [#323](https://github.com/Nimbly-Technologies/admin-lite/pull/323) · [#3079 — feat: gate Health Score peer-comparison line behind HEALTH_SCORE_PEER_COMPARISON flag (removal holdout)](https://github.com/Nimbly-Technologies/audit-admin/pull/3079)

#### Questionnaire poll hardening
- **Repo:** `audit-lite` · branch: `fix/audit-lite-v2-5hp-questionnaire-poll-throw`
- **What was done:** Stopped the questionnaire poll from throwing on benign no-update responses and hardened `questionnaireMap` access against a nullish map at runtime.
- **Commits:**
  - `759fe58d` — fix: harden questionnaireMap access against nullish map at runtime
  - `60dfb534` — fix: stop questionnaire poll throwing on benign no-update response

#### Schedule site-filter search debounce
- **Repo:** `audit-lite` · branch: `fix/audit-lite-v2-5hp-questionnaire-poll-throw`
- **What was done:** Debounced the schedule site-filter search to eliminate input jank; merged to main.
- **Commits:**
  - `8cba7ff6` — fix(schedule): debounce site filter search to stop input jank (#1794)
- **PR:** [#1794](https://github.com/Nimbly-Technologies/audit-lite/pull/1794)

### Notes
- `admin-lite` PR #323 and `audit-admin` PR #3079 were opened today but not yet merged — the peer-comparison holdout is pending review.
- The peer-comparison work is explicitly a *removal holdout*: the flag exists to keep the line available while the removal decision is finalized, so expect a follow-up to strip it once the holdout closes.

---

`★ Insight ─────────────────────────────────────`
- The two `audit-lite` poll fixes (`759fe58d`, `60dfb534`) are a classic defensive-coding pair: one guards the *data* (nullish map) and one guards the *control flow* (treating a no-update response as success rather than an error). Both are needed because a null-safe read alone won't stop a thrown poll cycle.
- The peer-comparison feature spans three repos because flag definitions (`admin-lite` feature-access screen), flag consumption (`audit-admin` UI gate), and a fallback selector all have to move together — a flag is only as safe as its weakest read site, hence the `refactor` commit adding a fallback.
`─────────────────────────────────────────────────`

Note: no Fizzy/Notion refs were present in any commit message, so the Tickets line is omitted; `calendarEvents` was empty, so no Meetings section.