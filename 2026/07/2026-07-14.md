## Work Log — 2026-07-14

> 12:10 pm – 01:48 pm · +66 / -0 lines · `audit-lite` (master)

### Summary
Shipped two independent improvements to `audit-lite`, both merged the same day. Enhanced Sentry observability by tagging each EAS (OTA) update as its own release, and fixed flow-event issue titling so Sentry groups events by message instead of collapsing them under "anonymous." Also reviewed an open PR on `nimbly-go-api`.

### Tasks & Changes

#### Per-OTA Sentry release tagging
- **Repo:** `audit-lite` · branch: `master`
- **What was done:** Configured the build/release pipeline to tag every EAS Update as a distinct Sentry release, so crashes and errors can be attributed to the specific OTA bundle that shipped them rather than a single rolling release.
- **Commits:**
  - `bdc8ca9d` — feat: tag each EAS Update as its own Sentry release (#1782)
- **PR:** [#1782 — feat: tag each EAS Update as its own Sentry release](https://github.com/Nimbly-Technologies/audit-lite/pull/1782)

#### Fix flow-event issue titling
- **Repo:** `audit-lite` · branch: `master`
- **What was done:** Corrected `captureFlowEvent` so Sentry issues are titled by their event message rather than defaulting to "anonymous," restoring meaningful grouping and searchability of flow-event issues.
- **Commits:**
  - `9fe4a80a` — fix: title captureFlowEvent issues by message, not "anonymous" (#1783)
- **PR:** [#1783 — fix: title captureFlowEvent issues by message, not "anonymous"](https://github.com/Nimbly-Technologies/audit-lite/pull/1783)

### Notes
- Reviewed PR [#128](https://github.com/Nimbly-Technologies/nimbly-go-api/pull/128) on `nimbly-go-api` (review submitted, no commits authored).
- Both feature branches (`feat/sentry-per-ota-release`, `fix/adr-020-flow-event-issue-title`) were merged and cleaned up the same day.

---

`★ Insight ─────────────────────────────────────`
- Both changes are pure observability plumbing, which is why the diff is `+66 / -0` — new tagging/titling logic added, nothing removed. Zero deletions is a strong signal these were additive config/instrumentation edits, not refactors.
- The two Sentry fixes are complementary: per-release tagging answers *"which OTA broke it,"* while message-based titling answers *"what actually broke"* — together they make crash triage on OTA-deployed React Native (EAS) apps far more actionable.
- The GitHub events reveal the full lifecycle even though only 2 commits landed: branch create → PR open → assign → merge → branch delete. Review-only work (the `nimbly-go-api` `PullRequestReviewEvent`) leaves no commit trace, so it belongs in Notes rather than Tasks.
`─────────────────────────────────────────────────`