## Work Log — 2026-07-03

> 10:46 am – 12:26 pm · +324 / -116 lines · `audit-lite` (fix/gateway-config-fetch-retry-backoff), `audit-admin` (master)

### Summary
Resolved a production-facing frontend resilience issue in `audit-admin` by adding recovery for stale-deploy chunk load failures and merging the fix into `master`. Also improved `audit-lite` gateway config fetching by adding retry/backoff behavior, tightening retries to transient failures only, and correcting retry attempt logging before opening the follow-up PR.

### Tasks & Changes

#### Stale deploy chunk-load error recovery
- **Repo:** `audit-admin` · branch: `master`
- **What was done:** Added a fix to recover from stale-deploy chunk load errors so the admin app can handle outdated asset references more gracefully after deployments.
- **Commits:**
  - `1e3799bf` — fix: recover from stale-deploy chunk load errors (AUDIT-ADMIN-1KV) (#3054)
- **PR:** [#3054 — fix: recover from stale-deploy chunk load errors (AUDIT-ADMIN-1KV)](https://github.com/Nimbly-Technologies/audit-admin/pull/3054)

#### Gateway config fetch retry and backoff hardening
- **Repo:** `audit-lite` · branch: `fix/gateway-config-fetch-retry-backoff`
- **What was done:** Hardened gateway config fetching by introducing retry with backoff, reducing retries to transient failures only, and improving warning logs to report the actual attempt count during fallback handling.
- **Commits:**
  - `60fb2b43` — fix: retry gateway config fetch with backoff, log as warning
  - `8a4b391d` — fix: only retry transient config-fetch failures (5xx/429/network)
  - `3d2cea8b` — fix: log real attempt count on config-fetch fallback
- **PR:** [#1746 — fix/gateway-config-fetch-retry-backoff](https://github.com/Nimbly-Technologies/audit-lite/pull/1746)

### Notes
`audit-lite` work was opened as PR `#1746` and appears to be in progress as of 2026-07-03.