## Work Log — 2026-07-13

> 11:27 am – 12:02 pm · +832 / -919 lines · `audit-lite` (master)

### Summary
Advanced the ADR-020 observability rework in `audit-lite`, replacing ad-hoc questionnaire business-logic spans with a cleaner flow-event telemetry approach. Shipped a reusable `captureFlowEvent` primitive for anchoring critical flows, migrated the questionnaire path onto it, and extended the release-sanity CI workflow with a dedicated staging build/OTA lane. All three PRs merged to `master`.

### Tasks & Changes

#### ADR-020: Flow-event telemetry migration
- **Repo:** `audit-lite` · branch: `master`
- **What was done:** Introduced a `captureFlowEvent` helper to anchor critical user flows, then swapped the questionnaire's business-logic spans over to the new flow-event telemetry — reducing span noise while preserving observability on the paths that matter.
- **Commits:**
  - `ad4cc8de` — refactor: remove questionnaire business-logic spans, add flow-event telemetry (#1768)
  - `37cb985e` — feat(observability): add captureFlowEvent for critical flow anchoring (#1761)
- **PR:** [#1768 — refactor: remove questionnaire business-logic spans, add flow-event telemetry](https://github.com/Nimbly-Technologies/audit-lite/pull/1768) · [#1761 — feat(observability): add captureFlowEvent for critical flow anchoring](https://github.com/Nimbly-Technologies/audit-lite/pull/1761)

#### Release-sanity staging lane
- **Repo:** `audit-lite` · branch: `master`
- **What was done:** Added a staging build/OTA lane to the release-sanity CI workflow, giving the pipeline a dedicated path to validate staging builds and over-the-air updates ahead of release.
- **Commits:**
  - `6de3f3ee` — feat: add staging build/OTA lane to release-sanity workflow (#1762)
- **PR:** [#1762 — feat: add staging build/OTA lane to release-sanity workflow](https://github.com/Nimbly-Technologies/audit-lite/pull/1762)

`★ Insight ─────────────────────────────────────`
- The `refactor` in #1768 depends on the `feat` in #1761 (`captureFlowEvent` must exist before the questionnaire spans can be migrated onto it), so grouping them into one task block reflects the real dependency — even though the commit subjects read as separate changes.
- Net line count (+832 / -919) being negative is a healthy signal for an observability refactor: you're deleting more instrumentation than you add, which usually means consolidating scattered spans into a single primitive.
`─────────────────────────────────────────────────`