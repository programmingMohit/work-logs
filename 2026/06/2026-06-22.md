## Work Log — 2026-06-22

> 04:07 pm – 05:09 pm · +102 / -32 lines · `nimbly-common` (chore/bump-1.95.55), `audit-lite` (fix/dup-imports-complete-login)

### Summary
Shipped a new `noFixPolicy` field at the org/schedule level in `nimbly-common` (PR #617) and cut a version bump release. In `audit-lite`, cleaned up duplicate imports and a duplicate `fetchAndCacheUserRoles` method, then repaired integration tests affected by the query-dedup/delta-autosave changes.

### Tasks & Changes

#### noFixPolicy field + resolution
- **Repo:** `nimbly-common` · branch: `chore/bump-1.95.55`
- **What was done:** Added a `noFixPolicy` field to the org/schedule schema with a `resolveNoFixPolicy` helper to derive the effective value, then bumped the package to 1.95.55 and merged the release.
- **Commits:**
  - `fa4da3c2` — chore: bump version to 1.95.55
  - `0d3cee44` — feat: add noFixPolicy org/schedule field + resolveNoFixPolicy (#617)
- **PR:** [#617 — feat: add noFixPolicy org/schedule field + resolveNoFixPolicy](https://github.com/Nimbly-Technologies/nimbly-common/pull/617)

#### Duplicate imports / method cleanup + test fixes
- **Repo:** `audit-lite` · branch: `fix/dup-imports-complete-login`
- **What was done:** Removed duplicate imports in `complete-login-usecase` and a redundant `fetchAndCacheUserRoles` method, then fixed integration tests broken by the query-dedup/delta-autosave changes.
- **Commits:**
  - `5f5ceda8` — fix: remove duplicate fetchAndCacheUserRoles method
  - `633f69a8` — test: fix integration tests for query-dedup/delta-autosave changes
  - `12692b1a` — fix: remove duplicate imports in complete-login-usecase

### Meetings
- **Weekly Check-in** — 12:30 PM–12:55 PM (25 min)
- **Delinda on leave** — all day

### Notes
- `audit-lite` PR #1732 was opened today but no merge event recorded yet — likely still in review.
- Both working branches (`chore/bump-1.95.55`, `fix/dup-imports-complete-login`) were deleted post-push, consistent with merged/cleaned-up work.

---

`★ Insight ─────────────────────────────────────`
- The `feat/nofix-policy-field-resolution` branch was created early (06:47 UTC) then deleted at 11:36 UTC, with the actual merge landing on `chore/bump-1.95.55` via PR #617 — the version-bump branch became the integration point for both the feature and the release.
- `resolveNoFixPolicy` paired with a stored field is a common config pattern: persist the raw setting at multiple levels (org + schedule), then resolve the effective value at read-time so precedence logic lives in one place.
`─────────────────────────────────────────────────`

Note: githubEvents references PR #619 (merged) and #1732 (opened), but the commit data only carries PR #617, so I only emitted the PR line where commit-level PR data exists per the rules.