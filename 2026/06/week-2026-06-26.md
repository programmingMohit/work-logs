## Weekly Summary — Week ending 2026-06-26

### Highlights
- Shipped `noFixPolicy` support across shared models and admin settings, covering org/schedule resolution in `nimbly-common`, admin-side configuration in `audit-admin`, and `SiteScheduleDetail` support in `nimbly-common`.
- Released two `nimbly-common` package bumps as part of the rollout: `1.95.55` and `1.95.57`.
- Cleaned up `audit-lite` auth code by removing duplicate imports and a redundant `fetchAndCacheUserRoles` method.
- Repaired `audit-lite` integration tests impacted by the query-dedup and delta-autosave changes.
- Opened and merged core `noFixPolicy` PRs in `nimbly-common`, and opened the related admin/settings and app follow-up work for review.

### By Repository
- **`nimbly-common`:** Added org/schedule-level `noFixPolicy` with resolution logic, added `noFixPolicy` to `SiteScheduleDetail`, and completed release version bumps to `1.95.55` and `1.95.57`, 4 commits, +81/-2 lines
- **`audit-admin`:** Added org-level `noFixPolicy` setting support in report settings and opened the related PR, 1 commit, +91/-9 lines
- **`audit-lite`:** Removed duplicate imports and redundant auth-role fetching logic, then fixed integration tests affected by recent query-dedup/delta-autosave changes, 3 commits, +29/-31 lines

### Stats
- Days worked: 4
- Total commits: 8
- Total lines: +201 / -42
- PRs: [#617](https://github.com/Nimbly-Technologies/nimbly-common/pull/617), [#622](https://github.com/Nimbly-Technologies/nimbly-common/pull/622), [#3047](https://github.com/Nimbly-Technologies/audit-admin/pull/3047), [#1732](https://github.com/Nimbly-Technologies/audit-lite/pull/1732)

### In Progress
`audit-lite` PR [#1732](https://github.com/Nimbly-Technologies/audit-lite/pull/1732) was opened this week, but no merge was recorded in the provided logs, so it appears to still be in review or awaiting follow-up.